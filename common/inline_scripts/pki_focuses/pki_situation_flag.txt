pki_selected_$FOCUS$ = {
    remove_country_flag = pki_selected_$FOCUS$
    prev = {
        if = {
            limit = {
                exists = this
            }
            set_variable = {
                which = pki_focus_speed
                value = $SPEED$
            }
            set_situation_flag = pki_$FOCUS$
        }
    }
}