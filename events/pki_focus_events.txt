namespace = pki_focus

# Checks if focuses should be bypassed
country_event = {
    id = pki_focus.001
    is_triggered_only = yes
    hide_window = yes

    immediate = {
        inline_script = {
            script = pki_focuses/pki_focuses
            PART = bypass_check
        }
    }
}

# Initializes focus time variables
country_event = {
    id = pki_focus.002
    is_triggered_only = yes
    hide_window = yes

    immediate = {
        inline_script = {
            script = pki_focuses/pki_focuses
            PART = time
        }
    }
}